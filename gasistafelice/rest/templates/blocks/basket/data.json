{% extends "blocks/base/data.json" %}

{% block table_rows %}
    {% for el in records %}
    [

        "{{el.order|escapejs}}",
        "{{el.supplier|escapejs}}",
        "{{el.product|escapejs}}",
        "{{el.amount|escapejs}}",
        "&#8364; {{ el.price|floatformat:2|escapejs }}",
        {# "{% filter escapejs %}{% bool_img stock.amount_available %}{% endfilter %}", #}

    ]
    {% if not forloop.last %}
    ,
    {% endif %}
    {% endfor %}

{% endblock %}

