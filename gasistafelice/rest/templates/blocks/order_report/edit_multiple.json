{% load basic_tags %}
{
    "sEcho": {{sEcho}},
    "iTotalRecords": {{iTotalRecords}},
    "iTotalDisplayRecords": {{iTotalDisplayRecords}},
    "aaData":[
    {% for order_product in records %}
    [
        "{{op.product|escapejs}}",
        "{{op.price|escapejs}}",
        "{{op.tot_gasmembers|escapejs}}",
        "{{op.tot_amount|escapejs}}",
        "&#8364; {{ op.tot_price|escapejs }}",
        "{{ op.field_enabled|escapejs }}",
    ]
    {% if not forloop.last %}
    ,
    {% endif %}
    {% endfor %}
    ],
    "gf_moreData": { {{ moreData|default:'' }} }
}
