{% extends "blocks/base/data.json" %}
{% load basic_tags %}

{% block table_rows %}
    {% for ss in records %}
    [

        "{{ss.id|escapejs}}",
        "{{el.supplier|escapejs}}",
        "{{el.product|escapejs}}",
        "{{el.ordered_amount}}",
        "{% filter escapejs %}{% bool_img ss.availability %}{% endfilter %}",
        "{{ss.urn|escapejs}}",

    ]
    {% if not forloop.last %}
    ,
    {% endif %}
    {% endfor %}

{% endblock %}
